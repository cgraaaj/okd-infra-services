# OKD router must read the TLS secret referenced by Route.spec.tls.externalCertificate.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: langfuse-tls-reader
  namespace: langfuse
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: ["langfuse-tls"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: langfuse-tls-reader-router
  namespace: langfuse
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: langfuse-tls-reader
subjects:
  - kind: ServiceAccount
    name: router
    namespace: openshift-ingress
