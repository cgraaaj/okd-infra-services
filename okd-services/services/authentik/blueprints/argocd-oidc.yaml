# Authentik OAuth2/OIDC provider + application for Argo CD prod.
# Client secret is injected by scripts/configure-oidc.sh before apply.
version: 1
metadata:
  name: argocd-oidc-prod
  labels:
    blueprints.goauthentik.io/description: "Argo CD prod OIDC (argocd.cgraaaj.in)"
entries:
  - id: argocd-oauth-provider
    model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: argocd-prod
    attrs:
      name: argocd-prod
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-openid]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-email]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-profile]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-groups]]
      client_type: confidential
      client_id: argocd-prod
      client_secret: __ARGOCD_OIDC_CLIENT_SECRET__
      redirect_uris:
        - matching_mode: strict
          url: https://argocd.cgraaaj.in/auth/callback
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
      sub_mode: user_username
      issuer_mode: per_provider

  - model: authentik_core.application
    identifiers:
      slug: argocd
    attrs:
      name: Argo CD Prod
      slug: argocd
      provider: !KeyOf argocd-oauth-provider
      meta_launch_url: https://argocd.cgraaaj.in/
      meta_description: OKD prod GitOps
      policy_engine_mode: any
